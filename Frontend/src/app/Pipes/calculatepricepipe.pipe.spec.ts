import { CalculatepricepipePipe } from './calculatepricepipe.pipe';

describe('CalculatepricepipePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatepricepipePipe();
    expect(pipe).toBeTruthy();
  });
});
